"""Market intelligence agent — Phase 2 (Person B)."""

from schemas import PortfolioInput, Article, MarketIntelOutput


def market_intel_agent(
    portfolio: PortfolioInput, news: list[Article]
) -> MarketIntelOutput:
    """Score sentiment, classify events, generate summaries."""
    raise NotImplementedError("Phase 2 — Person B")
