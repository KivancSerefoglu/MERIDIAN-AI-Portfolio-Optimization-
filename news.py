"""News + sentiment pipeline — Phase 1 (Person B)."""

from schemas import Article


def get_news(ticker: str) -> list[Article]:
    """Return top 3-5 recent headlines for a ticker."""
    raise NotImplementedError("Phase 1 — Person B")
